import React, { ReactElement } from 'react'

interface Props {

}

export default function testPlayground({}: Props): ReactElement {
    return (
        <section>
            <h1>For testing your components</h1>
        </section>
    )
}
